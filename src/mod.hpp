
#pragma once

#include "cdt.hpp"
#include "cte_v1.hpp"

namespace mod {
template<ctr::pixel Ty>
void detect(cte::v1::outline_t<Ty> const& current,
            cte::v1::outline_t<Ty> const& previous,
            cdt::offset_t adjustment) {
}
}; // namespace mod
